<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3>New players have joined your game at <em><%= @game.court.name %></em></h3>
    <div class="game_display">
      <p><%= "#{@game.time_info}" %></p>
      <p>Skill level:
        <%= @game.skill_level.nil? ? 'none' : @game.skill_level %>
      </p>
      <div class="game_players">
        <ul>
          <% @game.users.each do |user| %>
            <li><%= user.name %></li>
          <% end %>
        </ul>
      </div>
    </div>
    <h4>Check it out <a href="http://ballernycco.com/courts/<%= @game.court.id %>">here!</a></h4>
  </body>
</html>
