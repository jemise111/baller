<%= form_for([@court, @comment]) do |f| %>
 <div class="error_list">
    <ul>
      <% @comment.errors.full_messages.each do |m| %>
        <li><%= m %></li>
      <% end %>
    </ul>
  </div>
  <div class="field">
    <%= f.label :body, 'Comment' %><br>
    <%= f.text_area :body, maxlength: 200, required: true %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit for approval' %>
  </div>
<% end %>
