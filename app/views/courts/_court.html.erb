<!-- This is for admin display, not 'show' view -->
<div class="admin_stat">
  <p><%= court.id %> | </p>
  <p><%= court.name %> | </p>
  <p><%= court.location %> | </p>
  <p><%= link_to 'edit', edit_court_path(court) %></p>
  <p><%= link_to 'delete', court, method: :delete, data: {confirm: 'Are you sure? You cannot undo this action'} %></p>
</div>
